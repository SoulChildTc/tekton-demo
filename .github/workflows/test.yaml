name: GitHub Actions Demo
run-name: test cancle 🚀
on: [push]
jobs:
  job1:
    runs-on: self-hosted
    steps:
    - name: Step 1
      run: |
        env
    - name: Step 2
      run: docker images
